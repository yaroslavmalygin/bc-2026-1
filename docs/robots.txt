# Обход РАЗРЕШЁН намеренно.
#
# Из поиска страница выпадает мета-тегом <meta name="robots" content="noindex">
# в index.html. Если запретить обход здесь, краулер не прочитает страницу и не
# увидит этот тег — и URL всё равно сможет попасть в индекс по внешней ссылке,
# только без описания. То есть Disallow дал бы результат, обратный задуманному.

User-agent: *
Allow: /
